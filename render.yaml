services:
  - type: web
    name: chinese-dictation
    env: python
    buildCommand: : |
      pip install -r requirements.txt
    startCommand: "gunicorn 'run:app'"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DB_PATH
        value: progress.db

